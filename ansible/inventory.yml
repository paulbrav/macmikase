# Ansible inventory for local workstation configuration
# This playbook runs entirely on localhost

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"

